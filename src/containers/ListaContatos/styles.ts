import styled from 'styled-components'

export const ListaContatos = styled.table`
  margin: 0 auto;
  margin-top: 50px;
  border-collapse: separate;
  border-spacing: 2px;

  thead {
    font-size: 20px;
  }
`
